import CounterButton from "./counterButton";
const Counter = () => {
  return (
    <div style={{ display: "flex" }}>
      <CounterButton />
      <CounterButton />
      <CounterButton />
      <CounterButton />
    </div>
  );
};

export default Counter;
